# WARIKAN 開発ルール

## In-App Browser

In-App BrowserでHTMLを表示・更新する場合は、表示前に必ず以下を確認してください。

1. 今回変更・確認対象として指定されているHTMLファイル名を確認する。
2. In-App Browserで現在開いているURLを確認する。
3. URLが、今回変更・確認対象のHTMLファイルを指していることを確認する。
4. 同じ用途のモックHTMLが複数存在する場合、ファイル名だけで推測せず、今回の作業対象と一致していることを確認する。
5. 一致していない場合は、正しいHTMLファイルをIn-App Browserで開き直す。
6. 更新後、表示しているURLと対象HTMLファイル名を再度確認する。

特に、以下を禁止します。

- 変更したHTMLとは別のHTMLをIn-App Browserに表示したまま、変更結果として報告すること。
- ファイル名が似ているという理由だけで、表示対象を推測すること。
- 古いモックや別用途のモックを、今回の変更結果として表示すること。
- 表示対象が確認できていない状態で「反映済み」「表示済み」と報告すること。

複数のモックHTMLが存在する場合は、
「今回変更したファイル」と「In-App Browserで表示するファイル」が同一であることを確認してから表示してください。

作業完了時には、必要に応じて以下を報告してください。

- 変更したHTMLファイル名
- In-App Browserで表示したURL
- 両者が一致していること

## Diff確認・報告

Diffを報告する際は、HTML / CSS / JavaScriptごとに、何を変更したかと主なDiffを簡潔に示してください。

変更がない分類は「変更なし」とだけ記載してください。

最後に、以下を簡潔にまとめてください。

- 変更ファイル
- git diff --check の結果
- 意図しない差分の有無
- Commit / Push の実施有無

詳細な設計説明、影響範囲の長い解説、変更理由の繰り返しは、ユーザーから求められない限り省略してください。

## プロンプト作成時の報告ルール

ユーザーからGPT/Codex向けのプロンプト作成を依頼された場合は、以下の順序で提示する。

1. 最初に、そのプロンプトで指示している内容を簡潔な箇条書きで要約する
2. その下に、ユーザーがそのままコピーして送れるプロンプト本文を提示する

- 要約はできるだけ短くする
- 主要な作業内容・制約・停止地点のみ記載する
- プロンプト本文と同じ内容を長く繰り返さない
